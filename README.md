# PoodiCraft — האתר

אתר אחד, שני עמודים:

- `index.html` – עמוד הבית של הערוץ
- `portfolio.html` – תיק העבודות (טאמבניילים), מקושר מהכפתור "תיק עבודות"
- `projects.html` – המשחקים והפרויקטים, מקושר מהכרטיס "משחקים ופרויקטים" ומהתפריט
- `poodis-adventure.html` – עמוד המשחק Poodi's Adventure (עמוד נחיתה), מקושר מהתפריט "המשחק" ומהבאנר בעמוד הבית.
  הקישור להורדה (3 מקומות) ומספר הגרסה מופיעים בתוך הקובץ — כשמעלים גרסה חדשה מעדכנים גם שם.

שניהם משתמשים באותם קבצים: `style.css`, `script.js`, `sounds.js`,
ובתיקיות `logo/`, `thumbnails/`, `projects/`, `cursor/`, `sounds/`.
פתחו את `index.html` בדפדפן כדי לראות את האתר.

## מה נשאר למלא

באתר אין שום מידע על הערוץ מלבד השם, הלוגו, וקישורי יוטיוב/דיסקורד.
כל השאר מחכה לכם:

1. **משפט פתיחה** – ב-`index.html`, קטע Hero: החליפו את
   `<span class="fill-me">הוסיפו כאן משפט קצר על הערוץ</span>` בטקסט שלכם.
   אפשר גם לפתוח את ההערה שמעליו כדי להוסיף שורת כותרת שנייה.
2. **סרטונים** – ב-`script.js`, בתחילת הקובץ, בתוך `VIDEOS_TEXT`: סרטון בכל שורה —
   קודם הכתובת (או המזהה) מיוטיוב, רווח, ואז שם הסרטון:
   ```
   uPQi3wbNjps&t=6s        ישראל פארטי עונה 4 - פרק 1
   https://www.youtube.com/watch?v=ZP-nAIoxpPU   סרטון המועמדות שלי לנייטפול!
   ```
   אפשר להדביק כתובת מלאה, גם עם `&t=`. בלי פסיקים ובלי גרשיים. התמונה נלקחת
   מיוטיוב אוטומטית. הסדר בקובץ = הסדר באתר. כשהרשימה ריקה מוצגות משבצות ריקות.
3. **אודות** – הטקסט נמצא ב-`index.html`, קטע About, בתוך `<div class="about-body">`
   (כל שורה היא `<p>`).
4. **קישורים** – בקטע Links אפשר להוסיף כפתורים לרשתות נוספות.
   כפתור הדיסקורד פותח חלון עם "העתקת שם משתמש" ו"לשרת הדיסקורד שלי".
   **קישור השרת**: מלאו את `DISCORD_INVITE` בתחילת `script.js` (למשל
   `'https://discord.gg/XXXXXXX'`). עד אז הכפתור מוצג עם הערה שהקישור חסר.
   הטקסט שמתחת לכפתור נמצא ב-`DISCORD_NOTE` באותו מקום.
5. **פרויקטים** – לכל פרויקט יש מספר, ובתיקייה `projects/` שני קבצים באותו שם:
   - `project-1.jpg` – התמונה של הכרטיס (גם `.png` עובד). שלוש הראשונות מוצגות
     גם בכרטיס "משחקים ופרויקטים" בעמוד הבית.
   - `project-1.html` – **העמוד של הפרויקט**: כריכה רחבה עם השם והתגיות עליה,
     כפתורים, "מה זה", אריחי מאפיינים, שלבים ממוספרים, מציג צילומי מסך (תמונה
     גדולה + תמונות קטנות, חצים, לחיצה מגדילה), כרטיס פרטים בצד עם קישורים, וניווט לפרויקט הקודם/הבא. לחיצה על
     הכרטיס פותחת אותו. כל בלוק מסומן בהערה — ערכו, מחקו או שכפלו. סרטון מיוטיוב
     (רשות) מוסבר בהערה בקוד. צילומי מסך: `project-1-1.jpg`, `project-1-2.jpg`,
     `project-1-3.jpg`.
   - **השם והטקסט על הכרטיס** נמצאים ב-`projects.html` עצמו — כל כרטיס הוא בלוק
     `<a class="project-item">`; ערכו את `project-title`, `project-desc`, `project-tag`.
   **קבצים להורדה** (משחקים וכו') שמים בתיקייה `downloads/`, והכפתור מצביע אליהם:
   `<a href="../downloads/שם-הקובץ.exe" download class="btn btn-primary">להורדה</a>`.
   **התמונה לא מופיעה למרות שהשם נכון?** כנראה נכנסו לשם תווים בלתי-נראים בזמן
   שינוי השם בסייר (קורה כשהמקלדת בעברית). לחיצה כפולה על `projects/fix-names.bat`
   מנקה את זה.
   רוצים יותר מ-6? שכפלו בלוק כרטיס ב-`projects.html` ואת `project-6.html`
   ל-`project-7.html`. משפט הפתיחה של עמוד הפרויקטים ב-`projects.html` (`fill-me`).
6. **טאמבניילים** – שימו את התמונות בתיקייה `thumbnails/` עם השמות
   `thumb-1.jpg` … `thumb-5.jpg`. הן מוצגות גם בתיק העבודות וגם בכרטיס
   "תיק עבודות" בעמוד הבית. כדי להוסיף עוד, שכפלו בלוק
   `<figure class="gallery-item">` ב-`portfolio.html`.
7. **סמן העכבר** – הקבצים בתיקייה `cursor/`. הדפדפן מגביל סמן ל-32px,
   לכן האתר משתמש ב-`arrow-32.png` / `hand-32.png` (ו-`*-64.png` למסכי רטינה)
   שנוצרו מהתמונות המקוריות. אם מחליפים תמונה, צריך לייצר מחדש גרסת 32 ו-64.
8. **צלילים** – הקבצים בתיקייה `sounds/`: `click.wav` (לחיצה),
   `pop.wav` (העתקה / פתיחה / סגירה). אין צליל במעבר עכבר. הם לקוחים מחבילת
   ה-UI Audio החינמית של Kenney (רישיון CC0). כל החבילה נמצאת ב-`sounds/kenney-ui-audio/`
   כדי שתוכלו להאזין ולבחור אחרים: פתחו את `sounds/preview.html`, לחצו על
   הצלילים, ותגידו איזה אתם רוצים — או שמרו קובץ אחר באותו שם (`.wav`).
   עוצמת השמע נקבעת ב-`VOLUME` בתחילת `sounds.js`.
   כפתור ההשתקה מופיע בפינה התחתונה ושומר את הבחירה בדפדפן.

## המלצות (Firebase)

בתחתית תיק העבודות יש קטע המלצות: מבקרים נכנסים (Google או מייל+סיסמה),
כותבים המלצה עם דירוג, ויכולים לערוך ולמחוק רק את שלהם. החשבון
`PoodiCraftMc@gmail.com` הוא בעל האתר ויכול למחוק כל המלצה. עד שמחברים,
הקטע מציג הודעה ולא שובר כלום.

חיבור (פעם אחת, ~10 דקות):

1. https://console.firebase.google.com → **Add project** (שם חופשי, אפשר לכבות Analytics).
2. **Project settings** (גלגל שיניים) → **Your apps** → `</>` (Web) → Register app →
   מעתיקים את הערכים מהבלוק `firebaseConfig` לתוך `firebase-config.js`.
3. **Build → Authentication → Get started → Sign-in method**: מפעילים **Email/Password** ו-**Google**
   (ב-Google בוחרים מייל תמיכה ושומרים).
4. **Build → Firestore Database → Create database** (אשף בשלושה שלבים):
   - **Select edition**: Standard edition → Next.
   - **Database ID & location**: משאירים את ה-ID `(default)` (הקוד עובד מולו), בוחרים
     מיקום קרוב (למשל `europe-west1`; אי אפשר לשנות אחר כך) → Next.
   - **Configure**: בוחרים את אפשרות הכללים המאובטחת (secure / production) → Create.
   ואז בלשונית **Rules** מוחקים הכל, מדביקים את התוכן של `firestore.rules`, ו-**Publish**.
   זה השלב החשוב — הוא מה שקובע מי יכול לערוך ולמחוק.
5. **Authentication → Settings → Authorized domains → Add domain**: הדומיין שבו האתר עולה
   (למשל `poodicraft.github.io`). `localhost` כבר שם.

חשוב: כניסה עם Google לא עובדת כשפותחים את הקובץ ישירות מהתיקייה (file://) — רק
מאתר מארח או מ-localhost. מייל+סיסמה עובד גם ככה.
כדי להיות "בעל האתר" נכנסים עם Google עם החשבון PoodiCraftMc@gmail.com (או עם מייל+סיסמה
על אותו מייל אחרי אימות המייל). ליד השם יופיע התג "בעל האתר".
